# 自定义图片

NavigatorHub 支持放自己的截图、站点 favicon，构建时自动生效，无需改代码。

## 一、精选推荐大图（搜索框下方轮播）

位置：`src/assets/screenshots/`

把你自己的截图保存为 **文件名 = 下方 ID 之一**，支持 `.jpg / .jpeg / .png / .webp`。

| ID         | 对应的卡片       | 建议尺寸（横向图，越大越清晰） |
|------------|------------------|--------------------------------|
| deepseek   | AI 智能助手      | 1920×1080                      |
| github     | 开发者工具       | 1920×1080                      |
| dribbble   | 设计灵感         | 1920×1080                      |
| coursera   | 在线学习         | 1920×1080                      |
| bilibili   | 视频娱乐         | 1920×1080                      |
| notion     | 效率办公         | 1920×1080                      |

示例：
```
src/assets/screenshots/
  deepseek.png     ← 你自己截的 DeepSeek 首页
  bilibili.jpg     ← 你自己截的 B 站首页
  github.webp
```

放完图片后运行一次 `npm run build`，构建完成时首页精选推荐区会显示绿色徽章「X 张自定义图」，说明已正确识别。

如果没放图，会自动回退到 `public/screenshots/*.jpg` 默认占位图，再失败则显示精美的分类图标+渐变背景，永不空白。

---

## 二、站点卡片 favicon（中栏分类站点网格）

位置：`src/assets/favicons/`

支持 3 种命名方式，任一命中即可：
1. **站点 ID 命名**（最精确，推荐）—— 文件名 = `sites.ts` 中 `id` 字段
2. **主域名命名** —— 例如 `github.com.png`
3. **主域名去后缀** —— 例如 `github.png`

支持格式：`.ico / .png / .jpg / .jpeg / .svg / .webp`

常见站点 ID 速查（完整列表见 `src/config/sites.ts`）：

| ID              | 站点           |
|-----------------|----------------|
| s-baidu         | 百度           |
| s-google        | Google         |
| s-bing          | Bing           |
| s-ddg           | DuckDuckGo     |
| d-github        | GitHub         |
| d-vscode        | VS Code Web    |
| a-deepseek      | DeepSeek       |
| a-claude        | Claude         |
| a-mj            | Midjourney     |
| a-wx            | 通义万相       |
| so-bili         | 哔哩哔哩       |
| so-yt           | YouTube        |
| so-wb           | 微博           |
| so-zh           | 知乎           |
| v-iqiyi         | 爱奇艺         |
| v-tencent       | 腾讯视频       |
| v-spotify       | Spotify        |
| l-coursera      | Coursera       |
| l-juejin        | 掘金           |
| l-csdn          | CSDN           |
| o-notion        | Notion         |
| o-feishu        | 飞书           |
| o-tencent-doc   | 腾讯文档       |

示例：
```
src/assets/favicons/
  d-github.png            ← 按 ID 命名（推荐）
  github.com.ico          ← 按主域名命名
  bilibili.webp           ← 按去后缀命名
  百度.png               ← 不支持中文命名，请用上面 3 种方式
```

如果没放自定义图标，会自动回退到 DuckDuckGo 在线 API（实时抓取站点 favicon），再失败则显示 lucide 分类图标，永不空白。

放完图片后运行 `npm run build`，右键站点卡片若提示「自定义图标」，说明已命中。

---

## 三、构建生效流程

1. 把图片拖到 `src/assets/screenshots/` 或 `src/assets/favicons/` 中
2. 在项目根目录运行：
   ```powershell
   npm run build
   ```
3. 将 `dist/` 目录内所有文件（Ctrl+A）重新拖拽上传到 Netlify

Vite 会通过 `import.meta.glob` 在构建时自动枚举目录下的全部图片，按文件名匹配对应卡片，无需修改任何代码。
